<svg class="background-noise"
  ><filter id="pedroduarteisalegend"
    ><feTurbulence
      type="fractalNoise"
      baseFrequency="0.80"
      numOctaves="4"
      stitchTiles="stitch"
    ></feTurbulence></filter
  ><rect
    width="100%"
    height="100%"
    filter="url(#pedroduarteisalegend)"
  ></rect></svg
>

<div class="background-blur">
  <div class="background-color"></div>
</div>

<style lang="scss">
    .background-noise, .background-blur {
        position: fixed;

        width: 100%;
        height: 100vh;
        height: 100lvh;

        pointer-events: none;

        z-index: -1;
    }

    .background-noise {
        opacity: .65;
        mix-blend-mode: soft-light;

        z-index: 100002;
        isolation: isolate;
    }

    .background-blur {
      display: flex;
      align-items: center;
      .background-color {
          width: 100%;
          height: 15vh;
          height: 15lvh;
          background-color: hsl(var(--background-blur-color));
          filter: blur(200px);
      }
    }
</style>